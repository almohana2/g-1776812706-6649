# الأمان والخصوصية

## نموذج التهديد باختصار

النظام يحمل ثلاثة أشياء تستحق الحماية: **مفتاح Hydrawise** (من يملكه يستطيع
تشغيل الري)، **بيانات استهلاك الموقع**، و**رقم واتساب المستلم**. وهو يعمل على
خادم واحد بمستخدمين قلائل، فالتصميم يميل إلى البساطة القابلة للتدقيق.

## الأسرار

* لا يُخزَّن أي سر في المستودع. `.env` مستبعد في `.gitignore`، و`.env.example`
  يحمل أسماء المتغيرات فقط.
* الأسرار تُقرأ من البيئة عبر `SecretStr`، فلا تظهر في تتبّع الأخطاء.
* منقّح السجلات (`app/core/logging.py`) يستبدل أي `api_key=`/`token=` بـ`***`،
  ويستبدل قيم الأسرار المضبوطة أينما ظهرت، ويخفي أرقام الهواتف
  (`968****5218`).
* عميل Hydrawise **لا يسجّل عنوان URL كاملًا** لأن المفتاح يسافر في Query String؛
  يسجّل اسم النقطة وحالة HTTP والمدة فقط.
* الرقم التسلسلي للكنترولر يُعرض مخفيًا جزئيًا.

اختبار `tests/e2e/test_web.py::TestSecretsAndHeaders::test_ac_011_no_secret_leaks_into_any_page`
يمرّ على كل صفحة وكل استجابة JSON ويفشل إن ظهر سر.

## المصادقة والصلاحيات

* كلمات المرور: **Argon2id** (`time_cost=3`, `memory_cost=64MiB`, `parallelism=2`).
* الجلسة: كعكة موقّعة بمدة صلاحية 12 ساعة، `HttpOnly` و`SameSite=Lax`
  و`Secure` في الإنتاج.
* تغيير كلمة المرور أو تعطيل الحساب يرفع `session_epoch` فتسقط كل الجلسات
  القائمة فورًا.
* صلاحيتان: `admin` (كل شيء) و`viewer` (قراءة فقط). كل عملية متغيّرة تمر بـ
  `require_admin` و`verify_csrf`.
* رمز CSRF مشتق من معرّف الجلسة، فلا يصلح رمز جلسة لأخرى.
* تسجيل الدخول محدود بخمس محاولات لكل (عنوان، اسم) خلال خمس دقائق، ورسالة
  الخطأ واحدة لا تكشف وجود الحساب.

## الروابط العامة للتقارير

* الرمز عشوائي 256-bit (`secrets.token_urlsafe(32)`).
* تُخزَّن **بصمة SHA-256 فقط**؛ من يقرأ قاعدة البيانات لا يستطيع بناء الرابط.
* لكل رمز تاريخ انتهاء (افتراضيًا 40 يومًا)، وإصدار رمز جديد يُبطل السابق.
* الرابط المنتهي والرابط غير الموجود يعطيان الرد نفسه، فلا يكشف الفرق وجود
  تقرير من عدمه.
* الصفحة العامة تحمل `X-Robots-Tag: noindex` و`Cache-Control: no-store`، ولا
  تعرض أي زر إداري.

## ترويسات المتصفح

`Content-Security-Policy: default-src 'self'` — الصفحات مكتفية ذاتيًا: لا نصوص
خارجية ولا خطوط CDN ولا صور بعيدة. يُضاف `X-Content-Type-Options`,
`X-Frame-Options: DENY`, `Referrer-Policy: same-origin`, و`HSTS` في الإنتاج.

## سلامة التشغيل

* لا يستدعي التطبيق `setzone.php` ولا يعرض أي زر تشغيل أو إيقاف. الحارس
  `tests/unit/test_readonly_guard.py` يفحص الشجرة كلها.
* **هذا النظام ليس حماية من الجفاف.** تحذير «No Running Without Water» على
  المضخة يتطلب حماية كهربائية أو حساس مستوى مستقلًا تمامًا عن تطبيق تقارير.
* التيار المقنن `9.8 A` معلومة فنية تُعرض للاطلاع؛ ضبط الحماية الحرارية عمل
  كهربائي مختص، ولا يتدخل فيه التطبيق.

## البيانات والاحتفاظ

| البيانات | المدة |
|---|---|
| العينات الخام (`poll_samples`) | 90 يومًا افتراضيًا ثم تُحذف تلقائيًا |
| أحداث التشغيل والتقارير | تبقى بلا حذف تلقائي |
| سجل التدقيق | يبقى |
| النسخ الاحتياطية | 30 يومية + 12 شهرية |

لا تُرسَل البيانات إلى أي طرف غير Hydrawise وOpenWA المصرّح بهما.

## الإبلاغ عن ثغرة

راسل مالك النظام مباشرة. لا تفتح Issue عامًا بتفاصيل ثغرة قابلة للاستغلال.
